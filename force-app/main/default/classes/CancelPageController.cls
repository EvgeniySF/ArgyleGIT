public with sharing class CancelPageController {
 
    public static String registrationPage{ get; set; }
    
    public CancelPageController() {
      //  stripeCustomSettings = Stripe_Integration__c.getOrgDefaults();
       // registrationPage = 
    }
}